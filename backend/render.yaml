# Render deployment (free tier). See https://render.com/docs/blueprint-spec
services:
  - type: web
    name: dental-clinic-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
